apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "addgreen-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "addgreen-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "addgreen-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
